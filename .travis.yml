sudo: false
language: python
python:
  - "2.7"
  - "3.5"
install: pip install tox-travis
script: tox
addons:
  apt:
    packages:
    - libhdf5-serial-dev 
    - netcdf-bin 
    - libnetcdf-dev
    - python-numpy 
    - python-matplotlib
    - python-tox
install:
  - python setup.py install
