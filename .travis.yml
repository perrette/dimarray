language: python
sudo : false
addons:
  apt:
    packages:
    - libhdf5-serial-dev 
    - netcdf-bin 
    - libnetcdf-dev
    - python-numpy 
    - python-matplotlib
python:
  - "2.7"
notifications:
  email: false
before_install:
  - pip install pip --upgrade
  - pip install netcdf4
  # - pip install pandas  # optional requirement
# install cartopy
  # - pip install cython scipy Pillow
  # - pip install cartopy
# test coverage
  - pip install pytest pytest-cov
  - pip install coveralls
#optional dependencies
  # - pip install pandas  # to-pandas
# cartopy projections ?
# iris?
install:
  - python setup.py install

script: py.test --cov dimarray dimarray tests

after-success: coveralls
